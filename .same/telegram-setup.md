# 📱 Настройка Telegram Bot для приема заявок

## Шаг 1: Создание бота

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду `/newbot`
3. Придумайте **имя** для бота (например: "БТА ТОРГ Заявки")
4. Придумайте **username** для бота (должен заканчиваться на "bot", например: "bta_torg_bot")
5. **BotFather** отправит вам токен бота в формате:
   ```
   123456789:ABCdefGHIjklMNOpqrsTUVwxyz
   ```
6. **Скопируйте этот токен** - он понадобится на шаге 3

## Шаг 2: Получение Chat ID

### Вариант А - Получать заявки в личные сообщения:

1. Найдите вашего бота в Telegram по username (который вы создали в шаге 1)
2. Нажмите **Start** или отправьте `/start`
3. Отправьте боту любое сообщение (например: "тест")
4. Откройте в браузере ссылку (замените `YOUR_BOT_TOKEN` на ваш токен из шага 1):
   ```
   https://api.telegram.org/botYOUR_BOT_TOKEN/getUpdates
   ```
5. В ответе найдите строку вида:
   ```json
   "chat":{"id":123456789,"first_name":"Ваше имя"...}
   ```
6. **Скопируйте число после "id":** - это ваш Chat ID (например: `123456789`)

### Вариант Б - Получать заявки в группу/канал:

1. Создайте **группу** или **канал** в Telegram
2. Добавьте вашего бота в эту группу/канал
3. Сделайте бота **администратором** группы/канала
4. Напишите **любое сообщение** в эту группу/канал
5. Откройте в браузере ссылку (замените `YOUR_BOT_TOKEN` на ваш токен):
   ```
   https://api.telegram.org/botYOUR_BOT_TOKEN/getUpdates
   ```
6. В ответе найдите строку вида:
   ```json
   "chat":{"id":-100123456789,"title":"Название группы"...}
   ```
7. **Скопируйте число после "id":** - это Chat ID группы (начинается с минуса, например: `-100123456789`)

## Шаг 3: Настройка переменных окружения

1. Откройте файл `.env.local` в корне проекта `auto-import-russia`
2. Замените `YOUR_BOT_TOKEN_HERE` на токен бота из шага 1
3. Замените `YOUR_CHAT_ID_HERE` на Chat ID из шага 2
4. Сохраните файл

**Пример настроенного файла .env.local:**
```env
NEXT_PUBLIC_TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
NEXT_PUBLIC_TELEGRAM_CHAT_ID=123456789
```

Или для группы:
```env
NEXT_PUBLIC_TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
NEXT_PUBLIC_TELEGRAM_CHAT_ID=-100123456789
```

## Шаг 4: Перезапуск сервера

После настройки переменных окружения **обязательно перезапустите сервер разработки**:

1. Остановите текущий сервер (Ctrl+C)
2. Запустите снова: `bun run dev`

## Шаг 5: Тестирование

1. Откройте ваш сайт
2. Заполните форму "Оставить заявку"
3. Отправьте заявку
4. Проверьте, пришло ли сообщение в Telegram

## ✅ Готово!

Теперь все заявки с вашего сайта будут автоматически отправляться в Telegram!

## 🔧 Устранение проблем

### Заявки не приходят:

1. **Проверьте токен бота** - он должен быть скопирован полностью без пробелов
2. **Проверьте Chat ID** - для групп он должен начинаться с минуса
3. **Перезапустите сервер** - изменения в .env.local требуют перезапуска
4. **Проверьте консоль браузера** - там могут быть сообщения об ошибках
5. **Проверьте, что бот в группе имеет права администратора** (если используете группу)

### Сообщение "Сервис временно недоступен":

- Убедитесь, что вы заменили `YOUR_BOT_TOKEN_HERE` и `YOUR_CHAT_ID_HERE` на реальные значения
- Перезапустите сервер разработки

## 📝 Формат сообщений

Заявки будут приходить в Telegram в таком формате:

```
🚗 Новая заявка с сайта пригона авто!

👤 Имя: Иван Иванов
📱 Телефон: +7 (999) 123-45-67
💰 Бюджет: 2 000 000 ₽
💬 Комментарий:
Интересует BMW X5 2020 года

⏰ Время: 15.12.2025, 14:30:25
```

## 🔐 Безопасность

⚠️ **ВАЖНО:**
- Никогда не публикуйте файл `.env.local` в публичные репозитории
- Не делитесь токеном бота с посторонними
- Файл `.env.local` уже добавлен в `.gitignore`
