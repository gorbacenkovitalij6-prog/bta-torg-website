# 🎨 Руководство по анимациям и мобильному меню

## 📱 Мобильное бургер-меню

### Что реализовано:

**Компонент:** `src/components/MobileMenu.tsx`

**Возможности:**
- ✅ Плавное выезжающее меню справа
- ✅ Автоматическое закрытие при клике на пункт меню
- ✅ Плавная прокрутка к секции после закрытия
- ✅ Кнопка Telegram внутри меню
- ✅ Современный дизайн с тенями

**Как работает:**
1. На мобильных устройствах (<768px) появляется кнопка с иконкой бургера
2. При клике меню выезжает справа
3. Клик на любой пункт закрывает меню и прокручивает к нужной секции
4. Использует shadcn Sheet компонент

**Настройка пунктов меню:**

Откройте `src/components/MobileMenu.tsx` и отредактируйте массив `menuItems`:

```typescript
const menuItems = [
  { href: "#about", label: "О компании" },
  { href: "#services", label: "Услуги" },
  // ... добавьте свои пункты
]
```

---

## ✨ Анимации при прокрутке

### Что реализовано:

**Компонент:** `src/components/ScrollAnimation.tsx`

**Типы анимаций:**

1. **fade-up** — появление снизу вверх (по умолчанию)
2. **fade-in** — простое проявление
3. **fade-left** — появление слева
4. **fade-right** — появление справа
5. **scale-up** — увеличение с центра

### Как использовать:

#### Базовое использование:

```tsx
import { ScrollAnimation } from "@/components/ScrollAnimation";

<ScrollAnimation>
  <div>Ваш контент</div>
</ScrollAnimation>
```

#### С выбором типа анимации:

```tsx
<ScrollAnimation animation="scale-up">
  <Card>...</Card>
</ScrollAnimation>
```

#### С задержкой:

```tsx
{items.map((item, index) => (
  <ScrollAnimation
    key={index}
    animation="fade-up"
    delay={index * 100}
  >
    <Card>{item}</Card>
  </ScrollAnimation>
))}
```

### Параметры компонента:

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `animation` | string | `"fade-up"` | Тип анимации |
| `delay` | number | `0` | Задержка в миллисекундах |
| `className` | string | `""` | Дополнительные CSS классы |
| `children` | ReactNode | - | Контент для анимации |

### Технические детали:

**Как работает:**
1. Использует Intersection Observer API
2. Отслеживает появление элемента в viewport
3. Добавляет класс `animate-in` когда элемент виден
4. Плавные CSS transitions (0.8s cubic-bezier)

**Настройки Observer:**
```typescript
{
  threshold: 0.1,        // Запуск при 10% видимости
  rootMargin: "0px 0px -50px 0px"  // Отступ снизу 50px
}
```

### Где применяются анимации:

✅ **Заголовки секций** — fade-up
✅ **Карточки услуг** — fade-up с задержкой
✅ **Преимущества** — fade-up с задержкой
✅ **Примеры автомобилей** — scale-up с задержкой
✅ **Этапы работы** — fade-right с задержкой
✅ **FAQ** — fade-up
✅ **Калькулятор** — fade-up
✅ **Карта** — scale-up
✅ **Контакты** — fade-up

---

## 🎯 Кастомизация

### Изменение скорости анимации:

Откройте `src/app/globals.css`:

```css
.scroll-animation {
  transition: all 0.8s cubic-bezier(0.4, 0, 0.2, 1);
  /* Измените 0.8s на нужное значение */
}
```

### Изменение расстояния движения:

```css
.scroll-animation.fade-up {
  transform: translateY(30px); /* Измените 30px */
}
```

### Создание новой анимации:

1. Добавьте CSS в `globals.css`:

```css
.scroll-animation.rotate-in {
  transform: rotate(-10deg) scale(0.9);
}
```

2. Добавьте тип в `ScrollAnimation.tsx`:

```typescript
animation?: "fade-up" | "fade-in" | "rotate-in"
```

3. Используйте:

```tsx
<ScrollAnimation animation="rotate-in">
  <div>...</div>
</ScrollAnimation>
```

---

## 🚀 Производительность

**Оптимизации:**
- ✅ Используется нативный Intersection Observer (быстрее JS libraries)
- ✅ Анимации на CSS (GPU-accelerated)
- ✅ Observer отключается после первого появления
- ✅ Нет тяжелых зависимостей

**Best Practices:**
- Не анимируйте слишком много элементов одновременно
- Используйте задержки для последовательного появления
- Оптимальная задержка: 80-150ms между элементами

---

## 📱 Адаптивность

**Мобильное меню:**
- Появляется только на экранах < 768px
- На десктопе показывается обычная навигация

**Анимации:**
- Работают на всех устройствах
- Автоматически адаптируются к размеру экрана
- Не влияют на производительность мобильных устройств

---

## 🎨 Примеры использования

### Секция с карточками:

```tsx
<ScrollAnimation animation="fade-up">
  <h2>Заголовок</h2>
</ScrollAnimation>

<div className="grid grid-cols-3 gap-8">
  {items.map((item, i) => (
    <ScrollAnimation
      key={i}
      animation="scale-up"
      delay={i * 100}
    >
      <Card>{item}</Card>
    </ScrollAnimation>
  ))}
</div>
```

### Поэтапное появление:

```tsx
{steps.map((step, i) => (
  <ScrollAnimation
    animation="fade-right"
    delay={i * 150}
  >
    <StepItem step={step} />
  </ScrollAnimation>
))}
```

---

## ✨ Готово!

Сайт теперь имеет:
- 📱 Современное мобильное меню
- ✨ Плавные анимации при прокрутке
- 🎯 Отличный UX на всех устройствах

**Приятного использования!** 🚀
